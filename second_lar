def secondlargest(n,arr):
    arr.sort()
    for i in range(n-1,0,-1):
        if arr[i]>arr[i-1]:
            return arr[i-1]

n=int(input())
arr=list(map(int,input().split()))
print(secondlargest(n,arr))
